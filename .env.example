# Required - server will not start without this
# Generate a secure key: uuidgen or openssl rand -hex 32
GATEWAY_API_KEY=your-secret-api-key-here

GATEWAY_PORT=8787
GATEWAY_WORKDIR=/Users/jmwillis/code
GATEWAY_SESSION=main
GATEWAY_BOOT_CMD=claude
GATEWAY_USE_TMUX=true
GATEWAY_UPLOAD_DIR=/Users/jmwillis/.claude-gateway/uploads

# Session management
GATEWAY_SESSION_TTL_HOURS=4
GATEWAY_IDLE_TIMEOUT_MINS=30

# CORS (browser clients)
# If you access the gateway from a different origin (e.g. life-radar on iPad),
# you must allow that origin here so `/api/transcribe` works in Safari/PWA.
# Examples:
#   GATEWAY_CORS_ORIGINS=https://your-life-radar-domain.com
#   GATEWAY_CORS_ORIGINS=https://*.yourdomain.com
#   GATEWAY_CORS_ORIGINS=*
GATEWAY_CORS_ORIGINS=*

# Transcription
WHISPER_ENGINE=cpp
WHISPER_CPP_BIN=/opt/homebrew/opt/whisper-cpp/bin/whisper-cli
WHISPER_CPP_ARGS=-ng
WHISPER_MODEL=/Users/jmwillis/.local/share/whisper/ggml-base.en.bin
FFMPEG_BIN=ffmpeg
